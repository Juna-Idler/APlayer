<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="APlayer.SliderMarkerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:APlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="UserControl_Loaded"
    SizeChanged="UserControl_SizeChanged">

    <Grid Background="Transparent">
        <Grid.ContextFlyout>
            <MenuFlyout x:Name="MenuFlyout">
            </MenuFlyout>
        </Grid.ContextFlyout>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>
        
        <Canvas x:Name="MarkerCanvas" Grid.ColumnSpan="2">
        </Canvas>
        <Slider x:Name="SliderMin" Minimum="0" Maximum="100" Value="0" IsEnabled="False" />
        <Slider x:Name="SliderMax" Minimum="0" Maximum="100" Value="100" IsEnabled="False" />

        <Slider x:Name="MarkerSlider" Visibility="Collapsed"
                Minimum="0" 
                Maximum="100"
                TickFrequency="60"
                TickPlacement="Inline"
                IsThumbToolTipEnabled="False"
                
                ValueChanged="MarkerSlider_ValueChanged"
                PointerWheelChanged="MarkerSlider_PointerWheelChanged"
                ContextRequested="MarkerSlider_ContextRequested"
        />

    </Grid>
</UserControl>
