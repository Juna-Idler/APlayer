<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="APlayer.VideoViewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:APlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <MediaPlayerElement x:Name="MPElement" AreTransportControlsEnabled="True" AutoPlay="True"
                            Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Grid.ColumnSpan="2"/>
        <Button Click="Button_Click" Grid.Row="0" Grid.Column="0">
            <FontIcon  FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE72B;"/>
        </Button>

    </Grid>
</UserControl>
