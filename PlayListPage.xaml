<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="APlayer.PlayListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:APlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="Grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="BackButton" 
                Style="{StaticResource NavigationBackButtonNormalStyle}"
                IsEnabled="False" 
                ToolTipService.ToolTip="Back"/>
        <ListView x:Name="FoldersView" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                      Background="AliceBlue"
                  HorizontalAlignment="Stretch"
                  CanDragItems="True" CanReorderItems="True" AllowDrop="True" SelectionMode="Single"
                  >
            <ListView.ItemTemplate>
                <DataTemplate >
                    <Grid Margin="2" Padding="4" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="64"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Button>
                        </Button>
                        <TextBlock Text="" Grid.Column="1" />
                        <TextBlock Text="" Grid.Column="2" TextTrimming="CharacterEllipsis" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
